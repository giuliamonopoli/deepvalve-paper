### The data is separated into regression data and segmentation data.
While the annotations of the cardiac structures are from the same source, segmentation data requires the creation of masks to train and test.
